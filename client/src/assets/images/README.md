# Images

AI-generated travel imagery used across the UI. Referenced via `constants/images.js`.

Slugs used by the backend seed data: `agra-taj`, `agra-fort`, `jaipur`, `delhi`, `varanasi`, `amritsar`, `hotel`, `hero`.
